<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zto.starunion.tools.dal.mapper.StrategyProjectMapper">
    <select id="listAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from strategy_project
    </select>
    <select id="listByStrategyId" resultType="com.zto.starunion.tools.pojo.vo.StrategyProjectVo">
        select
        a.id as id , a.project_id as projectId, a.git_branch as gitBranch, a.update_date as updateDate,
        p.name as projectName, p.git_url as gitUrl
        from strategy_project as a
        left join project as p on a.project_id = p.id
        where a.strategy_id = #{strategyId}
    </select>

    <select id="getByProjectIdAndStrategyId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from strategy_project
        where strategy_id = #{strategyId} and project_id = #{projectId}
    </select>

</mapper>
