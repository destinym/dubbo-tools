<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zto.starunion.tools.dal.mapper.AppMapper">
    <select id="listAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from app
    </select>
    <select id="getByAppIdAndProjectId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from app
        where app_id = #{appId} and project_id = #{projectId}
    </select>

    <select id="listByProjectId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from app
        where project_id = #{projectId}
    </select>



</mapper>